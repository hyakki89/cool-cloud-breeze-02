{% comment %}
  Connexion client
{% endcomment %}

<div class="container py-8 max-w-md mx-auto">
  <div class="card p-8">
    <h1 class="text-2xl font-bold text-center mb-6">Connexion</h1>
    
    {% form 'customer_login' %}
      <div class="mb-4">
        <label for="customer_email" class="block text-sm font-medium mb-2">Email</label>
        <input 
          type="email" 
          name="customer[email]" 
          id="customer_email"
          class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-blue-500"
          required
        >
      </div>
      
      <div class="mb-6">
        <label for="customer_password" class="block text-sm font-medium mb-2">Mot de passe</label>
        <input 
          type="password" 
          name="customer[password]" 
          id="customer_password"
          class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-blue-500"
          required
        >
      </div>
      
      <button type="submit" class="w-full btn mb-4">Se connecter</button>
      
      <div class="text-center">
        <a href="{{ routes.account_register_url }}" class="text-blue-600 hover:underline">
          Créer un compte
        </a>
      </div>
    {% endform %}
  </div>
</div>